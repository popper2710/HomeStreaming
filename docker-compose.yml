version: "3"

services:
  golang:
    tty: true
    build: .
    volumes:
      - ./app:/go/src/github.com/home_streaming/app
    ports:
      - 5002:5002
      - 3000:3000
    command: sh -c "cd app && realize start --server"

